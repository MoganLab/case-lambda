# (scheme case-lambda)

R7RS 标准 case-lambda 库。

## 导入

```scheme
(import (scheme case-lambda))
; 或
(import (srfi srfi-16))
```

## 说明

`(scheme case-lambda)` 库导出单个绑定
`case-lambda`，用于定义具有多种参数模式的函数。

这与 SRFI-16 完全一致。

## 文档

详细文档请参考 [SRFI-16: Case-lambda](../srfi/srfi-16.md)。
